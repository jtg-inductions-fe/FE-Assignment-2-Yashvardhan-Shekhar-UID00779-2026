import { Container, Stack, styled } from '@mui/material';

export const StyledNotFoundContainer = styled(Container)({
    height: '90vh',
    display: 'flex',
    alignItems: 'center',
});

export const ContentStack = styled(Stack)({
    textAlign: 'center',
});
