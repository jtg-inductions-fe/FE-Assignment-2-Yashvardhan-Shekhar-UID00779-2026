# allows Yarn to use any Git repository as a package source.
approvedGitRepositories:
    - '**'

# allows packages to run lifecycle scripts
enableScripts: true

# Define how Node packages should be installed, disable yarn Plug N play installation strategy
nodeLinker: node-modules

# after how many minutes you can use published packages
npmMinimalAgeGate: 0

# should not be ignored by git to maintain same version of yarn
yarnPath: .yarn/releases/yarn-4.18.0.cjs
